import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.xueyuanhuang.balancesheet",
  appName: "Net Worth",
  webDir: "out",
};

export default config;
